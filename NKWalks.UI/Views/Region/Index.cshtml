@model IEnumerable<NKWalks.UI.Models.DTO.RegionDTO>
@{
}

<h1 class="mt-3">Regions</h1>

<div class="d-flex justify-content-end">
    <a class="btn btn-secondary" asp-controller="Region" asp-action="Add">Add Region</a>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Code</th>
        </tr>
    </thead> 
    <tbody>
        @foreach (var region in Model)
        {
            <tr>
                <td>@region.Id</td>
                <td>@region.Name</td>
                <td>@region.Code</td>
                <td>
                    <a asp-controller="Region" asp-action="Edit" asp-route-id="@region.Id" class="btn btn-light" >Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>
